# 배열과 리스트

> 한 줄 요약: 여러 데이터를 순차적으로 저장하는 가장 기본적인 자료구조

## 📌 목차

- [개요](#개요)
- [배열 (Array)](#배열-array)
- [리스트 (List)](#리스트-list)
- [배열 vs 리스트](#배열-vs-리스트)
- [ArrayList vs LinkedList](#arraylist-vs-linkedlist)
- [실전 활용](#실전-활용)
- [주의사항](#주의사항)

---

## 개요

### 순차 자료구조란?

데이터를 일렬로 나열하여 저장하는 자료구조입니다. 인덱스를 통해 각 요소에 접근할 수 있습니다.

### 언제 사용하는가?

#### ✅ 적합한 상황

- 데이터를 순서대로 저장해야 할 때
- 인덱스로 빠르게 접근해야 할 때
- 데이터를 순회해야 할 때
- 크기가 고정되어 있거나 예측 가능할 때 (배열)

#### ❌ 부적합한 상황

- 중간 삽입/삭제가 빈번할 때 (배열)
- 크기를 자주 변경해야 할 때 (배열)
- 키-값 쌍으로 저장해야 할 때 → Map 사용

---

## 배열 (Array)

### 개념

**배열**은 같은 타입의 데이터를 연속된 메모리 공간에 저장하는 자료구조입니다.

```
인덱스:  0    1    2    3    4
배열:  [10] [20] [30] [40] [50]
       ↑                      ↑
     시작                    끝
메모리: 연속된 공간
```

### 특징

| 특징           | 설명                    |
|--------------|-----------------------|
| **고정 크기**    | 생성 시 크기 결정, 변경 불가     |
| **연속 메모리**   | 메모리 상에 연속적으로 배치       |
| **빠른 접근**    | 인덱스로 O(1) 접근          |
| **느린 삽입/삭제** | 중간 삽입/삭제 시 이동 필요 O(n) |

### 시간 복잡도

| 연산          | 시간 복잡도 | 설명         |
|-------------|--------|------------|
| 접근 (Access) | O(1)   | 인덱스로 바로 접근 |
| 탐색 (Search) | O(n)   | 순차 탐색 필요   |
| 삽입 (Insert) | O(n)   | 뒤 요소들을 이동  |
| 삭제 (Delete) | O(n)   | 뒤 요소들을 이동  |

### Java 배열

#### 기본 사용법

```java
public class ArrayExample {
    public static void main(String[] args) {
        // 1. 선언 및 초기화
        int[] arr1 = new int[5];           // 크기 5, 기본값 0
        int[] arr2 = {10, 20, 30, 40, 50}; // 초기값 지정
        int[] arr3 = new int[]{1, 2, 3};   // 크기 자동 결정

        // 2. 접근
        System.out.println(arr2[0]);  // 10
        arr2[0] = 100;                // 값 변경

        // 3. 길이
        System.out.println(arr2.length);  // 5

        // 4. 순회
        for (int i = 0; i < arr2.length; i++) {
            System.out.println(arr2[i]);
        }

        // 향상된 for문
        for (int num : arr2) {
            System.out.println(num);
        }
    }
}
```

#### 2차원 배열

```java
public class TwoDimensionalArray {
    public static void main(String[] args) {
        // 1. 선언 및 초기화
        int[][] matrix = new int[3][4];  // 3행 4열

        int[][] arr = {
                {1, 2, 3},
                {4, 5, 6},
                {7, 8, 9}
        };

        // 2. 접근
        System.out.println(arr[0][0]);  // 1
        System.out.println(arr[1][2]);  // 6

        // 3. 순회
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr[i].length; j++) {
                System.out.print(arr[i][j] + " ");
            }
            System.out.println();
        }

        // 향상된 for문
        for (int[] row : arr) {
            for (int num : row) {
                System.out.print(num + " ");
            }
            System.out.println();
        }
    }
}
```

#### 배열 복사

```java
import java.util.Arrays;

public class ArrayCopy {
    public static void main(String[] args) {
        int[] original = {1, 2, 3, 4, 5};

        // 1. Arrays.copyOf() - 새 배열 생성
        int[] copy1 = Arrays.copyOf(original, original.length);

        // 2. System.arraycopy() - 기존 배열에 복사
        int[] copy2 = new int[5];
        System.arraycopy(original, 0, copy2, 0, original.length);

        // 3. clone() - 얕은 복사
        int[] copy3 = original.clone();

        // ❌ 잘못된 복사 (참조만 복사)
        int[] copy4 = original;  // 같은 배열을 가리킴!
    }
}
```

#### 유용한 메서드 (Arrays 클래스)

```java
import java.util.Arrays;

public class ArraysUtil {
    public static void main(String[] args) {
        int[] arr = {5, 2, 8, 1, 9};

        // 정렬
        Arrays.sort(arr);  // [1, 2, 5, 8, 9]

        // 이진 탐색 (정렬된 배열에서)
        int index = Arrays.binarySearch(arr, 5);  // 2

        // 배열 채우기
        int[] filled = new int[5];
        Arrays.fill(filled, 100);  // [100, 100, 100, 100, 100]

        // 배열 비교
        int[] arr1 = {1, 2, 3};
        int[] arr2 = {1, 2, 3};
        boolean isEqual = Arrays.equals(arr1, arr2);  // true

        // 문자열로 변환
        System.out.println(Arrays.toString(arr));  // [1, 2, 5, 8, 9]
    }
}
```

### Kotlin 배열

#### 기본 사용법

```kotlin
fun main() {
    // 1. 선언 및 초기화
    val arr1 = arrayOf(10, 20, 30, 40, 50)
    val arr2 = intArrayOf(1, 2, 3, 4, 5)        // 기본형 배열
    val arr3 = Array(5) { 0 }                    // 크기 5, 초기값 0
    val arr4 = Array(5) { i -> i * 2 }          // [0, 2, 4, 6, 8]

    // 2. 접근
    println(arr1[0])       // 10
    arr1[0] = 100         // 값 변경

    // 3. 길이
    println(arr1.size)    // 5

    // 4. 순회
    for (i in arr1.indices) {
        println(arr1[i])
    }

    // 향상된 for문
    for (num in arr1) {
        println(num)
    }

    // forEachIndexed
    arr1.forEachIndexed { index, value ->
        println("arr[$index] = $value")
    }
}
```

#### 2차원 배열

```kotlin
fun main() {
    // 1. 선언 및 초기화
    val matrix = Array(3) { IntArray(4) }  // 3행 4열

    val arr = arrayOf(
        intArrayOf(1, 2, 3),
        intArrayOf(4, 5, 6),
        intArrayOf(7, 8, 9)
    )

    // 2. 접근
    println(arr[0][0])  // 1
    println(arr[1][2])  // 6

    // 3. 순회
    for (row in arr) {
        for (num in row) {
            print("$num ")
        }
        println()
    }

    // forEach
    arr.forEach { row ->
        row.forEach { num ->
            print("$num ")
        }
        println()
    }
}
```

#### 함수형 스타일

```kotlin
fun main() {
    val arr = intArrayOf(1, 2, 3, 4, 5)

    // map: 변환
    val doubled = arr.map { it * 2 }  // [2, 4, 6, 8, 10]

    // filter: 필터링
    val evens = arr.filter { it % 2 == 0 }  // [2, 4]

    // reduce: 집계
    val sum = arr.reduce { acc, i -> acc + i }  // 15

    // any, all, none
    val hasEven = arr.any { it % 2 == 0 }     // true
    val allPositive = arr.all { it > 0 }      // true
    val noNegative = arr.none { it < 0 }      // true

    // sortedBy, sortedByDescending
    val sorted = arr.sortedDescending()       // [5, 4, 3, 2, 1]

    // take, drop
    val first3 = arr.take(3)                  // [1, 2, 3]
    val last2 = arr.takeLast(2)               // [4, 5]
    val without2 = arr.drop(2)                // [3, 4, 5]
}
```

---

## 리스트 (List)

### 개념

**리스트**는 크기가 가변적인 순차 자료구조입니다. Java/Kotlin에서는 인터페이스로 제공되며, 주로 ArrayList와 LinkedList로 구현됩니다.

### List 인터페이스

```
List (인터페이스)
  ├── ArrayList    (배열 기반)
  ├── LinkedList   (노드 기반)
  └── Vector       (동기화, Legacy)
```

### Java List

#### ArrayList 기본 사용법

```java
import java.util.ArrayList;
import java.util.List;

public class ArrayListExample {
    public static void main(String[] args) {
        // 1. 생성
        List<Integer> list = new ArrayList<>();
        ArrayList<String> names = new ArrayList<>();

        // 초기 용량 지정 (성능 최적화)
        List<Integer> optimized = new ArrayList<>(100);

        // 2. 추가
        list.add(10);           // [10]
        list.add(20);           // [10, 20]
        list.add(1, 15);        // [10, 15, 20] - 인덱스 1에 삽입

        // 3. 접근
        int value = list.get(0);     // 10
        list.set(0, 100);            // [100, 15, 20] - 값 변경

        // 4. 삭제
        list.remove(0);              // [15, 20] - 인덱스로 삭제
        list.remove(Integer.valueOf(20));  // [15] - 값으로 삭제

        // 5. 크기
        int size = list.size();      // 1
        boolean isEmpty = list.isEmpty();  // false

        // 6. 포함 여부
        boolean contains = list.contains(15);  // true

        // 7. 순회
        for (int i = 0; i < list.size(); i++) {
            System.out.println(list.get(i));
        }

        for (int num : list) {
            System.out.println(num);
        }
    }
}
```

#### List 변환 및 정렬

```java
import java.util.*;
import java.util.stream.Collectors;

public class ListOperations {
    public static void main(String[] args) {
        List<Integer> list = Arrays.asList(5, 2, 8, 1, 9);

        // 1. 정렬
        Collections.sort(list);                    // [1, 2, 5, 8, 9]
        Collections.sort(list, Collections.reverseOrder());  // [9, 8, 5, 2, 1]

        // 2. 역순
        Collections.reverse(list);                 // [1, 2, 5, 8, 9]

        // 3. 섞기
        Collections.shuffle(list);

        // 4. 최대/최소
        int max = Collections.max(list);
        int min = Collections.min(list);

        // 5. 배열로 변환
        Integer[] array = list.toArray(new Integer[0]);

        // 6. Stream 활용
        List<Integer> doubled = list.stream()
                .map(n -> n * 2)
                .collect(Collectors.toList());

        List<Integer> evens = list.stream()
                .filter(n -> n % 2 == 0)
                .collect(Collectors.toList());

        // 7. 불변 리스트 생성
        List<Integer> immutable = List.of(1, 2, 3);  // Java 9+
        // immutable.add(4);  // UnsupportedOperationException!
    }
}
```

### Kotlin List

#### 기본 사용법

```kotlin
fun main() {
    // 1. 읽기 전용 List (Immutable)
    val readOnlyList = listOf(1, 2, 3, 4, 5)
    // readOnlyList.add(6)  // 컴파일 에러!

    // 2. 변경 가능한 MutableList
    val mutableList = mutableListOf(1, 2, 3)

    // 추가
    mutableList.add(4)              // [1, 2, 3, 4]
    mutableList.add(1, 10)          // [1, 10, 2, 3, 4]
    mutableList += 5                // [1, 10, 2, 3, 4, 5]

    // 접근
    val first = mutableList[0]      // 1
    mutableList[0] = 100            // [100, 10, 2, 3, 4, 5]

    // 삭제
    mutableList.removeAt(0)         // [10, 2, 3, 4, 5]
    mutableList.remove(10)          // [2, 3, 4, 5]
    mutableList -= 2                // [3, 4, 5]

    // 크기
    println(mutableList.size)       // 3
    println(mutableList.isEmpty())  // false

    // 포함 여부
    println(3 in mutableList)       // true

    // 3. 빈 리스트
    val emptyList = emptyList<Int>()
    val emptyMutableList = mutableListOf<Int>()
}
```

#### 함수형 스타일

```kotlin
fun main() {
    val list = listOf(1, 2, 3, 4, 5)

    // map: 변환
    val doubled = list.map { it * 2 }  // [2, 4, 6, 8, 10]

    // filter: 필터링
    val evens = list.filter { it % 2 == 0 }  // [2, 4]

    // find: 첫 번째 요소 찾기
    val firstEven = list.find { it % 2 == 0 }  // 2
    val firstBig = list.find { it > 10 }       // null

    // partition: 조건으로 분리
    val (even, odd) = list.partition { it % 2 == 0 }
    // even = [2, 4], odd = [1, 3, 5]

    // groupBy: 그룹화
    val grouped = list.groupBy { it % 2 }
    // {1=[1, 3, 5], 0=[2, 4]}

    // sorted: 정렬
    val sorted = list.sortedDescending()  // [5, 4, 3, 2, 1]
    val sortedBy = list.sortedBy { -it }  // [5, 4, 3, 2, 1]

    // distinct: 중복 제거
    val duplicates = listOf(1, 2, 2, 3, 3, 3)
    val unique = duplicates.distinct()    // [1, 2, 3]

    // flatten: 중첩 리스트 평탄화
    val nested = listOf(listOf(1, 2), listOf(3, 4))
    val flat = nested.flatten()           // [1, 2, 3, 4]

    // reduce, fold: 집계
    val sum = list.reduce { acc, i -> acc + i }     // 15
    val sumFold = list.fold(0) { acc, i -> acc + i }  // 15
}
```

#### 리스트 생성 및 변환

```kotlin
fun main() {
    // 1. List 생성
    val list1 = List(5) { it }           // [0, 1, 2, 3, 4]
    val list2 = List(5) { it * 2 }       // [0, 2, 4, 6, 8]

    // 2. 범위로 생성
    val range = (1..5).toList()          // [1, 2, 3, 4, 5]
    val step = (1..10 step 2).toList()   // [1, 3, 5, 7, 9]

    // 3. 배열에서 변환
    val array = intArrayOf(1, 2, 3)
    val fromArray = array.toList()       // [1, 2, 3]

    // 4. 문자열 분리
    val str = "a,b,c,d"
    val split = str.split(",")           // ["a", "b", "c", "d"]

    // 5. 리스트 결합
    val list3 = listOf(1, 2, 3)
    val list4 = listOf(4, 5, 6)
    val combined = list3 + list4         // [1, 2, 3, 4, 5, 6]

    // 6. 중첩 리스트
    val matrix = List(3) { row ->
        List(3) { col -> row * 3 + col }
    }
    // [[0, 1, 2], [3, 4, 5], [6, 7, 8]]
}
```

---

## 배열 vs 리스트

### 비교표

| 특징         | 배열 (Array)  | 리스트 (ArrayList)  |
|------------|-------------|------------------|
| **크기**     | 고정          | 가변               |
| **메모리**    | 연속된 공간      | 내부적으로 배열 사용      |
| **접근 속도**  | O(1)        | O(1)             |
| **삽입/삭제**  | 불가능 (크기 고정) | 가능 O(n)          |
| **타입**     | 기본형 가능      | 객체만 가능 (Wrapper) |
| **메모리 효율** | 더 효율적       | 오버헤드 존재          |
| **사용 편의성** | 낮음          | 높음               |

### 언제 무엇을 사용할까?

#### 배열 사용

```java
// ✅ 크기가 고정되어 있을 때
int[] scores = new int[5];  // 시험 점수 5개

// ✅ 성능이 중요할 때 (메모리, 속도)
int[] cache = new int[1000000];

// ✅ 기본형 데이터를 다룰 때
int[] numbers = {1, 2, 3, 4, 5};  // int (4 bytes)
// vs
List<Integer> list = Arrays.asList(1, 2, 3, 4, 5);  // Integer 객체 (더 많은 메모리)
```

#### 리스트 사용

```java
// ✅ 크기가 동적으로 변할 때
List<String> names = new ArrayList<>();
names.

add("Alice");
names.

add("Bob");   // 크기 자동 증가

// ✅ 삽입/삭제가 빈번할 때
List<Integer> queue = new ArrayList<>();
queue.

add(1);
queue.

remove(0);

// ✅ 편리한 메서드가 필요할 때
list.

contains(5);
list.

indexOf(10);
list.

subList(0,3);
```

---

## ArrayList vs LinkedList

### 내부 구조 비교

#### ArrayList

```
내부 배열:
[10] [20] [30] [40] [50] [ ] [ ] [ ]
                         ↑
                      capacity
특징:
- 내부적으로 배열 사용
- 용량 부족 시 자동 확장 (보통 1.5배)
- 인덱스 접근 빠름 O(1)
- 중간 삽입/삭제 느림 O(n)
```

#### LinkedList

```
노드 체인:
[10|●]→[20|●]→[30|●]→[40|●]→[50|null]

특징:
- 노드(데이터+포인터)로 연결
- 크기 제한 없음
- 인덱스 접근 느림 O(n)
- 중간 삽입/삭제 빠름 O(1)
```

### 성능 비교

| 연산                  | ArrayList | LinkedList |
|---------------------|-----------|------------|
| get(index)          | O(1)      | O(n)       |
| add(element)        | O(1)*     | O(1)       |
| add(index, element) | O(n)      | O(n)       |
| remove(index)       | O(n)      | O(n)       |
| remove(element)     | O(n)      | O(n)       |

*ArrayList의 add는 용량 부족 시 O(n) (배열 복사)

### 사용 예시

#### ArrayList

```java
import java.util.ArrayList;
import java.util.List;

public class ArrayListUseCase {
    public static void main(String[] args) {
        // ✅ 인덱스 접근이 많을 때
        List<Integer> list = new ArrayList<>();
        for (int i = 0; i < 1000; i++) {
            list.add(i);
        }

        // 빠른 접근
        for (int i = 0; i < 100; i++) {
            int value = list.get(i);  // O(1)
        }
    }
}
```

#### LinkedList

```java
import java.util.LinkedList;
import java.util.Queue;

public class LinkedListUseCase {
    public static void main(String[] args) {
        // ✅ 큐 구현 (앞/뒤 삽입/삭제가 많을 때)
        Queue<Integer> queue = new LinkedList<>();

        queue.offer(1);      // 뒤에 추가
        queue.offer(2);
        queue.offer(3);

        while (!queue.isEmpty()) {
            System.out.println(queue.poll());  // 앞에서 제거
        }
    }
}
```

### Kotlin에서의 선택

```kotlin
fun main() {
    // ArrayList (기본)
    val arrayList = arrayListOf(1, 2, 3)

    // LinkedList
    val linkedList = LinkedList<Int>()
    linkedList.add(1)
    linkedList.addFirst(0)  // 앞에 추가
    linkedList.addLast(2)   // 뒤에 추가

    // 대부분의 경우 ArrayList로 충분
    val list = mutableListOf(1, 2, 3)  // 내부적으로 ArrayList
}
```

---

## 실전 활용

### 코딩 테스트 필수 패턴

#### 1. 누적합 (Prefix Sum)

```java
public class PrefixSum {
    /**
     * 구간 합을 O(1)에 구하기
     */
    public static void main(String[] args) {
        int[] arr = {1, 2, 3, 4, 5};
        int[] prefixSum = new int[arr.length + 1];

        // 누적합 계산
        for (int i = 0; i < arr.length; i++) {
            prefixSum[i + 1] = prefixSum[i] + arr[i];
        }
        // prefixSum = [0, 1, 3, 6, 10, 15]

        // 구간 [1, 3]의 합 = prefixSum[4] - prefixSum[1]
        int sum = prefixSum[4] - prefixSum[1];  // 2+3+4 = 9
        System.out.println(sum);
    }
}
```

```kotlin
fun main() {
    val arr = intArrayOf(1, 2, 3, 4, 5)
    val prefixSum = IntArray(arr.size + 1)

    for (i in arr.indices) {
        prefixSum[i + 1] = prefixSum[i] + arr[i]
    }

    // 구간 [1, 3]의 합
    val sum = prefixSum[4] - prefixSum[1]  // 9
    println(sum)
}
```

#### 2. 투 포인터

```java
public class TwoPointers {
    /**
     * 두 수의 합이 target인 쌍 찾기 (정렬된 배열)
     */
    public static boolean hasPairWithSum(int[] arr, int target) {
        int left = 0;
        int right = arr.length - 1;

        while (left < right) {
            int sum = arr[left] + arr[right];

            if (sum == target) {
                return true;
            } else if (sum < target) {
                left++;
            } else {
                right--;
            }
        }

        return false;
    }
}
```

```kotlin
fun hasPairWithSum(arr: IntArray, target: Int): Boolean {
    var left = 0
    var right = arr.size - 1

    while (left < right) {
        val sum = arr[left] + arr[right]

        when {
            sum == target -> return true
            sum < target -> left++
            else -> right--
        }
    }

    return false
}
```

#### 3. 슬라이딩 윈도우

```java
public class SlidingWindow {
    /**
     * 크기 k인 윈도우의 최대 합
     */
    public static int maxSumSubarray(int[] arr, int k) {
        if (arr.length < k) return -1;

        // 첫 윈도우 합
        int windowSum = 0;
        for (int i = 0; i < k; i++) {
            windowSum += arr[i];
        }

        int maxSum = windowSum;

        // 윈도우 슬라이딩
        for (int i = k; i < arr.length; i++) {
            windowSum += arr[i] - arr[i - k];
            maxSum = Math.max(maxSum, windowSum);
        }

        return maxSum;
    }
}
```

```kotlin
fun maxSumSubarray(arr: IntArray, k: Int): Int {
    if (arr.size < k) return -1

    var windowSum = arr.take(k).sum()
    var maxSum = windowSum

    for (i in k until arr.size) {
        windowSum += arr[i] - arr[i - k]
        maxSum = maxOf(maxSum, windowSum)
    }

    return maxSum
}
```

### 실무 활용

#### 데이터 그룹화

```java
import java.util.*;
import java.util.stream.Collectors;

public class DataGrouping {
    static class Student {
        String name;
        int score;

        Student(String name, int score) {
            this.name = name;
            this.score = score;
        }
    }

    public static void main(String[] args) {
        List<Student> students = Arrays.asList(
                new Student("Alice", 85),
                new Student("Bob", 92),
                new Student("Charlie", 78),
                new Student("David", 92)
        );

        // 점수별 그룹화
        Map<Integer, List<Student>> byScore = students.stream()
                .collect(Collectors.groupingBy(s -> s.score));

        // 점수별 학생 수
        Map<Integer, Long> countByScore = students.stream()
                .collect(Collectors.groupingBy(
                        s -> s.score,
                        Collectors.counting()
                ));
    }
}
```

```kotlin
data class Student(val name: String, val score: Int)

fun main() {
    val students = listOf(
        Student("Alice", 85),
        Student("Bob", 92),
        Student("Charlie", 78),
        Student("David", 92)
    )

    // 점수별 그룹화
    val byScore = students.groupBy { it.score }
    // {85=[Alice], 92=[Bob, David], 78=[Charlie]}

    // 점수별 학생 수
    val countByScore = students.groupingBy { it.score }.eachCount()
    // {85=1, 92=2, 78=1}

    // 90점 이상 학생
    val topStudents = students.filter { it.score >= 90 }

    // 평균 점수
    val avgScore = students.map { it.score }.average()
}
```

---

## 주의사항

### ⚠️ 흔한 실수

#### 1. 배열 범위 초과

```java
// ❌ ArrayIndexOutOfBoundsException
int[] arr = new int[5];
int value = arr[5];  // 인덱스는 0~4

// ✅ 범위 체크
if(index >=0&&index<arr.length){
int value = arr[index];
}
```

```kotlin
// ❌ Exception
val arr = intArrayOf(1, 2, 3)
val value = arr[3]

// ✅ 안전한 접근
val value = arr.getOrNull(3)  // null 반환
val value2 = arr.getOrElse(3) { 0 }  // 기본값 반환
```

#### 2. 리스트 순회 중 수정

```java
// ❌ ConcurrentModificationException
List<Integer> list = new ArrayList<>(Arrays.asList(1, 2, 3, 4, 5));
for(
int num :list){
        if(num %2==0){
        list.

remove(Integer.valueOf(num));  // 위험!
        }
        }

// ✅ Iterator 사용
Iterator<Integer> iterator = list.iterator();
while(iterator.

hasNext()){
        if(iterator.

next() %2==0){
        iterator.

remove();
    }
            }

// ✅ removeIf 사용 (Java 8+)
            list.

removeIf(num ->num %2==0);
```

```kotlin
// ❌ ConcurrentModificationException
val list = mutableListOf(1, 2, 3, 4, 5)
for (num in list) {
    if (num % 2 == 0) {
        list.remove(num)  // 위험!
    }
}

// ✅ filter로 새 리스트 생성
val filtered = list.filter { it % 2 != 0 }

// ✅ removeAll 사용
list.removeAll { it % 2 == 0 }
```

#### 3. 얕은 복사 vs 깊은 복사

```java
// ❌ 얕은 복사 (참조만 복사)
List<int[]> original = new ArrayList<>();
original.

add(new int[] {
    1, 2, 3
});

List<int[]> shallow = new ArrayList<>(original);
shallow.

get(0)[0]=100;  // original도 변경됨!

// ✅ 깊은 복사
List<int[]> deep = new ArrayList<>();
for(
int[] arr :original){
        deep.

add(arr.clone());
        }
```

```kotlin
// ❌ 얕은 복사
val original = mutableListOf(intArrayOf(1, 2, 3))
val shallow = original.toMutableList()
shallow[0][0] = 100  // original도 변경됨!

// ✅ 깊은 복사
val deep = original.map { it.clone() }.toMutableList()
```

### 💡 팁

#### 1. ArrayList 초기 용량 지정

```java
// ❌ 용량 부족 시 자주 재할당
List<Integer> list = new ArrayList<>();  // 기본 용량 10
for(
int i = 0;
i< 10000;i++){
        list.

add(i);  // 여러 번 배열 복사 발생
}

// ✅ 초기 용량 지정
List<Integer> optimized = new ArrayList<>(10000);
for(
int i = 0;
i< 10000;i++){
        optimized.

add(i);  // 재할당 없음
}
```

#### 2. 불변 리스트 활용

```kotlin
// ✅ 읽기 전용으로 충분하면 List 사용
fun getNames(): List<String> {
    return listOf("Alice", "Bob", "Charlie")
    // 외부에서 수정 불가
}

// ❌ 불필요하게 MutableList 반환
fun getNamesMutable(): MutableList<String> {
    return mutableListOf("Alice", "Bob")
    // 외부에서 수정 가능 (의도하지 않은 동작)
}
```

#### 3. 배열 초기화 패턴

```java
// 1차원 배열
int[] arr = new int[5];
Arrays.

fill(arr, -1);  // 모두 -1로 초기화

// 2차원 배열
int[][] matrix = new int[3][4];
for(
int[] row :matrix){
        Arrays.

fill(row, -1);
}
```

```kotlin
// 1차원 배열
val arr = IntArray(5) { -1 }

// 2차원 배열
val matrix = Array(3) { IntArray(4) { -1 } }
```

---

## 연습 문제

### 추천 문제 (난이도별)

#### 🟢 Easy

1. [백준 10818 - 최소, 최대](https://www.acmicpc.net/problem/10818) - 배열 기본
2. [백준 2562 - 최댓값](https://www.acmicpc.net/problem/2562) - 배열 탐색
3. [프로그래머스 - 배열 뒤집기](https://school.programmers.co.kr/learn/courses/30/lessons/120821) - 배열 조작

#### 🟡 Medium

1. [백준 11659 - 구간 합 구하기 4](https://www.acmicpc.net/problem/11659) - 누적합
2. [백준 2003 - 수들의 합 2](https://www.acmicpc.net/problem/2003) - 투 포인터
3. [백준 1940 - 주몽](https://www.acmicpc.net/problem/1940) - 투 포인터 + 정렬

#### 🔴 Hard

1. [백준 2143 - 두 배열의 합](https://www.acmicpc.net/problem/2143) - 누적합 + 해시
2. [백준 1806 - 부분합](https://www.acmicpc.net/problem/1806) - 슬라이딩 윈도우

---

## 참고 자료

### Java 공식 문서

- [Arrays](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/Arrays.html)
- [ArrayList](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/ArrayList.html)
- [List](https://docs.oracle.com/en/java/javase/17/docs/api/java.base/java/util/List.html)

### Kotlin 공식 문서

- [Collections Overview](https://kotlinlang.org/docs/collections-overview.html)
- [List](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin.collections/-list/)

### 추천 학습 자료

- [바킹독 배열 강의](https://blog.encrypted.gg/927)
- [코딩테스트 대비 자료구조](https://github.com/encrypted-def/basic-algo-lecture)

---

**작성일:** 2026-01-18
**최종 수정일:** 2026-01-18
**작성자:** 민순기
